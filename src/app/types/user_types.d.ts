type User = {
    /**
    * User id as defined by the database
    */
    id: number,
    /**
     * User email as entered when created
     */
    email: string,
    first_name: string,
    last_name: string
}